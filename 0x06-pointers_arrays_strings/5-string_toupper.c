#include "main.h"
/**
 * string_topper - change all lowercase to uppercase
 * @n: pointer
 * return: n
 */
char *string_toupper(char *)
{
	int i;

	i = 0;
	while (n[i] != '\0')
	{
		if (n[i] >= 'a' && n[i] <= 'a')
			n[i] = n[i] - 32;
		i++;
	}
	return (n);
}
